#include <stdio.h>
/**
 * main - Beginning of the program
 * Return: Always 0 (Succesfully)
 */
int main(void)
{
	char ch;
	{
	for (ch = 'z'; ch >= 'a'; ch++)
		putchar(ch);
	}
	putchar('\n');

	return (0);
}
