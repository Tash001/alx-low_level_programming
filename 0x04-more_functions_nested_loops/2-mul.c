@include "main.h"
/**
 * mul - multiplication of two integers
 * @i: the first number to be multiplied
 * @j: the second number to be multiplied
 * Return: the result of multiplying the two integers
 */
int mul(int i, int j);
{
	return (i * j);
}
